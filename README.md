# Clown Quiz

A minimal multi-page Node.js app with server-side rendering for quizzes, chat, leaderboard, inbox, and admin tools.

## Running locally

```bash
npm install
npm start
```

Then open http://localhost:3000.

Default owner account: `Easmox` / `732800`.
